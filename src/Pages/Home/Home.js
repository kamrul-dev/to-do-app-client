import React from 'react';
import Login from '../../Authentication/Login';
import AppEntering from './AppEntering';

const Home = () => {
    return (
        <div>
            <Login></Login>
        </div>
    );
};

export default Home;